# Pennsieve API credentials (required — never commit .env)
PENNSIEVE_API_KEY=
PENNSIEVE_API_SECRET=

# API host
PENNSIEVE_API_HOST=https://api2.pennsieve.io

# Workflow configuration
WORKFLOW_ID=
DATASET_ID=

# Optional: seconds to wait between triggering runs (default 1)
DELAY_BETWEEN_RUNS=1
